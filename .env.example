# AI Case Analyst (optional). Without this, the app runs fully and the
# analyst falls back to a deterministic on-device brief.
# Set this ONLY in your serverless host's env — never in client code.
ANTHROPIC_API_KEY=sk-ant-...
